// Merge dos subvetores v[l..m-1] e v[m..r-1]
i = l, j = m, k = l;
while (i < m && j < r) {
    // Se originalmente v[l..r] era crescente ou decrescente
    // a escolha entre 'if' e 'else' se torna determinística
    if (v_aux[i] <= v_aux[j]) {
        v[k++] = v_aux[i++];
    } else {
        v[k++] = v_aux[j++];
    }
}