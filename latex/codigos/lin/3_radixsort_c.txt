void RadixsortC(int n, int *v) {
    int *cont = AlocaVetor(10);
    int *v_aux = AlocaVetor(n);
    for (int p = 1; MAX_EL / p > 0; p *= 10) {
        for (int i = 0; i < 10; i++) {
            cont[i] = 0;
        }
        for (int i = 0; i < n; i++) {
            cont[(v[i] / p) % 10]++;
        }
        for (int i = 1; i < 10; i++) {
            cont[i] += cont[i - 1];
        }
        for (int i = n - 1; i >= 0; i--) {
            int j = (v[i] / p) % 10;
            v_aux[cont[j] - 1] = v[i];
            cont[j]--;
        }
        for (int i = 0; i < n; i++) {
            v[i] = v_aux[i];
        }
    }
    free(cont);
    free(v_aux);
}